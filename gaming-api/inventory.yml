all:
  children:
    backend:
      hosts:
        genesis-backend:
          ansible_connection: containers.podman.podman
          ansible_python_interpreter: /usr/local/bin/python3.11

    database:
      hosts:
        genesis-db:
          ansible_connection: containers.podman.podman
