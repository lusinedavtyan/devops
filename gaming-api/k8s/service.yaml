apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: genesis
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      nodePort: 30080
